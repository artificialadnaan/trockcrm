console.error(
  "scripts/backfillProjectNumbers.ts is retired. bidBoardProjectNumber is sourced from Bid Board sync only; use scripts/refixDealNumbers.ts for internal dealNumber repair."
);
process.exit(1);
