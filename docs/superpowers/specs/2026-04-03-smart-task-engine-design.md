# T Rock CRM Smart Task Engine Design

**Date:** 2026-04-03
**Status:** Draft for review
**Scope:** Task lifecycle, automation centralization, assignment, prioritization, and related production/runtime fixes

---

## 1. Goal

Turn the existing task system into a deterministic, rule-driven smart task engine that:

- fixes current runtime and lifecycle gaps
- centralizes automated task generation behind one service
- adds provenance, scheduling, and dependency tracking to tasks
- improves assignment and prioritization without introducing a full AI planner
- closes the loop when tasks are completed so the CRM state stays current

This design keeps manual tasks and system-generated tasks in the same task model. The system remains deterministic, testable, and explainable.

---

## 2. Why This Direction

The current implementation already has working task CRUD, completion, and worker-driven automation. The main problem is fragmentation:

- lifecycle support is incomplete in both schema and UI
- automated task creation is spread across worker jobs and event handlers
- deduplication is text-based and brittle
- assignment logic is duplicated and shallow
- the API spec and UI behavior lag behind the live implementation

The recommended approach is to evolve the existing task module in place and route all automated task generation through a shared rule evaluation service. This is lower risk than introducing a second recommendation system or a generic workflow engine.

---

## 3. Phase Structure

### Phase A: Runtime and Lifecycle Corrections

Deliver the shortest path to a reliable task system.

- Fix any production hosting/path issue that is directly actionable within this initiative, especially where frontend/API routing breaks task flows.
- Fix task assignee display so UI consistently shows display names instead of raw UUIDs.
- Expand task lifecycle to support real status transitions for `scheduled`, `in_progress`, `waiting_on`, and `blocked`.
- Add UI controls and API validation for those transitions.
- Add director/admin assignee filtering on the tasks page, matching backend capabilities.
- Update stale API spec definitions for task schemas, statuses, and filters.
- Route all system notification emails generated by this area to `adnaan.iqbal@gmail.com`.

### Phase B: Schema and API Extension

Extend tasks so the system can explain why a task exists, when it should surface, and what it depends on.

New task fields:

- `office_id`
- `origin_rule`
- `source_rule`
- `source_event`
- `dedupe_key`
- `reason_code`
- `entity_snapshot`
- `scheduled_for`
- `waiting_on`
- `blocked_by`
- `started_at`

Additional close-loop persistence:

- `task_resolution_state` table

These fields apply to both current task API responses and internal rule evaluation. `task_resolution_state` is the durable close-loop store for dedupe suppression and rule outcome memory.

### Phase C: Centralized Rule-Driven Generation

Introduce a typed `task_rules` configuration and a task rule evaluation service. Existing automated task creation paths are migrated to call the evaluator instead of inserting tasks directly.

Initial migration targets:

- inbound email follow-up creation
- stale deal follow-up creation
- close-date reminder generation
- first outreach touchpoint generation
- cadence-based follow-up generation
- cold lead warming generation

### Phase D: Smarter Assignment and Prioritization

Assignment becomes a deterministic strategy chain:

1. manual override
2. deal owner
3. contact-linked rep
4. recent actor
5. office fallback

Priority becomes a score derived from:

- due proximity
- deal stage risk
- stale age
- unread inbound activity
- deal value

The score is mapped into existing priority bands: `urgent`, `high`, `normal`, `low`.

### Phase E: Close-Loop Behavior

When a task completes, the system updates related CRM state and suppresses duplicate system suggestions.

Initial close-loop behaviors:

- update related contact/deal recency signals
- record task provenance-aware completion outcomes
- prevent duplicate open suggestions for the same `dedupe_key`

---

## 4. Lifecycle Model

### 4.1 Statuses

Task status becomes:

- `pending`
- `scheduled`
- `in_progress`
- `waiting_on`
- `blocked`
- `completed`
- `dismissed`

### 4.2 Semantics

- `pending`: actionable now, not yet started
- `scheduled`: not actionable until `scheduled_for`
- `in_progress`: actively being worked; first transition into this state sets `started_at`
- `waiting_on`: paused pending an external dependency, person, reply, or event
- `blocked`: cannot proceed because a blocker exists and needs explicit resolution
- `completed`: work finished
- `dismissed`: intentionally closed without completion

### 4.3 Companion Fields

`waiting_on` and `blocked_by` are companion explanation/reference fields, not replacement statuses.

- `waiting_on` is required when status is `waiting_on`
- `blocked_by` is required when status is `blocked`
- `scheduled_for` is required when status is `scheduled`
- `started_at` is set automatically when entering `in_progress` for the first time

Office contract:

- every task persists `office_id`
- `office_id` is set at creation time and never changes after insert, even if the task is later reassigned across users with multi-office access
- office-local time calculations for activation and overdue semantics always resolve through the persisted task office
- office-level fallback assignment settings also resolve through the persisted task office

Creation-time `office_id` derivation order:

1. current tenant office for request-driven manual task creation
2. event payload office id for domain-event-driven system tasks
3. worker iteration office id for office-scoped cron scans
4. related entity office only if it is tenant-consistent and explicitly available in rule context

If `office_id` cannot be derived deterministically, the evaluator returns a structured skip reason and does not create the task.

### 4.4 Transition Rules

Allowed transitions:

- `scheduled` -> `pending`
- `scheduled` -> `dismissed`
- `pending` -> `in_progress`
- `pending` -> `waiting_on`
- `pending` -> `blocked`
- `pending` -> `completed`
- `pending` -> `dismissed`
- `in_progress` -> `waiting_on`
- `in_progress` -> `blocked`
- `in_progress` -> `completed`
- `in_progress` -> `dismissed`
- `waiting_on` -> `pending`
- `waiting_on` -> `in_progress`
- `waiting_on` -> `blocked`
- `waiting_on` -> `completed`
- `waiting_on` -> `dismissed`
- `blocked` -> `pending`
- `blocked` -> `in_progress`
- `blocked` -> `waiting_on`
- `blocked` -> `completed`
- `blocked` -> `dismissed`

Automatic transitions:

- `scheduled` -> `pending` when `scheduled_for <= now` in office-local time and the activation worker promotes it

Disallowed behavior:

- editing completed or dismissed tasks through the standard task edit flow
- reopening completed/dismissed tasks through the normal edit flow

---

## 5. Task Data Model Changes

### 5.1 Existing Fields Kept

The current task identity and linkage model stays intact:

- title/description/type/priority/status
- assignee and creator references
- optional deal/contact/email linkage
- due date/time and reminder fields

### 5.2 New Fields

#### `origin_rule`

String identifier for the rule that originally created the task. Null for manual tasks. This never changes after insert and is the primary provenance field for audit/debugging.

#### `source_rule`

String identifier for the rule that most recently refreshed or materially updated the task. Null for manual tasks.

Examples:

- `deal.close_date.follow_up`
- `contact.first_outreach.missing`
- `email.inbound.unassigned`

#### `source_event`

String identifier for the triggering event or worker pass.

Examples:

- `email.received`
- `deal.stage.changed`
- `cron.daily_task_generation`

#### `dedupe_key`

Stable unique key used to suppress duplicate active tasks created for the same business reason.

Examples:

- `email-reply-needed:{emailId}`
- `close-date-follow-up:{dealId}:{expectedCloseDate}`
- `touchpoint-first-outreach:{contactId}`

#### `reason_code`

Compact code describing why the task exists. Used in UI messaging, analytics, and later tuning.

Examples:

- `reply_needed`
- `close_date_approaching`
- `touchpoint_cadence_overdue`
- `stale_deal`

#### `entity_snapshot`

JSON snapshot of the key data the rule used when generating the task. This supports explainability without requiring later joins to unchanged records.

This is not unconstrained JSON. It follows a versioned envelope:

- `schema_version`
- `entity_type`
- `entity_id`
- `summary`
- `signals`

Example contents:

- deal number and stage
- contact display name
- last activity timestamp
- unread inbound count

#### `scheduled_for`

Timezone-aware timestamp indicating when a scheduled task becomes active. All persistence uses UTC. Activation comparisons use the office-local timezone when deciding whether the task should surface as active work.

#### `waiting_on`

Structured JSON reference describing what the task is waiting on.

Shape:

- `schema_version`
- `kind`
- `label`
- `ref_type`
- `ref_id`

Examples:

- `{ schema_version: 1, kind: "person_reply", label: "Customer reply", ref_type: "email", ref_id: "{messageId}" }`
- `{ schema_version: 1, kind: "internal_handoff", label: "Proposal revision from estimating", ref_type: "team", ref_id: "estimating" }`

#### `blocked_by`

Structured JSON reference describing the blocker.

Shape:

- `schema_version`
- `kind`
- `label`
- `ref_type`
- `ref_id`

Examples:

- `{ schema_version: 1, kind: "approval", label: "Missing deal stage approval", ref_type: "deal_stage", ref_id: "{dealId}" }`
- `{ schema_version: 1, kind: "integration", label: "Procore project not created", ref_type: "deal", ref_id: "{dealId}" }`

#### `started_at`

Timestamp set when the task first enters `in_progress`.

#### `task_resolution_state`

New tenant-scoped table for durable rule outcome memory and duplicate suppression.

Columns:

- `id`
- `task_id`
- `origin_rule`
- `dedupe_key`
- `resolution_status`
- `resolution_reason`
- `resolved_at`
- `suppressed_until`
- `entity_snapshot`
- `created_at`
- `updated_at`

Use:

- persist provenance-aware completion outcomes
- suppress regenerated suggestions for the same business reason
- preserve close-loop state even after the original task becomes terminal

Resolution state contract:

- authoritative business key is `origin_rule` plus `dedupe_key`
- allowed `resolution_status` values are `completed`, `dismissed`, `suppressed`, `expired`
- `completed` means the business reason was satisfied and should suppress regeneration for the configured suppression window
- `dismissed` means the user intentionally closed the task without satisfying the business reason and does not suppress regeneration unless the rule explicitly marks the dismissal reason as suppressible
- `suppressed` means regeneration is intentionally blocked until `suppressed_until`
- `expired` is a derived evaluation state, not a persisted terminal value; it means the suppression window has elapsed and the rule may generate a fresh task again if conditions still match

Suppression rules:

- completion of a system-generated task creates or updates a resolution row with `resolution_status = completed`
- the default suppression window is rule-defined and must be explicit in the `task_rules` config
- dismissal only creates a suppressing resolution row when the rule explicitly declares that dismissal reason as suppressible
- regeneration checks `task_resolution_state` before materializing a new task for the same `origin_rule` and `dedupe_key`

Persistence contract:

- add a unique index on `task_resolution_state(origin_rule, dedupe_key)`
- resolution upserts always target that unique business key
- when `suppressed_until <= now`, evaluators treat the row as `expired` without requiring a separate worker to persist an expiry transition

---

## 6. Rule Engine Design

### 6.1 Core Principle

All automated task creation flows through one service:

- worker cron jobs may detect candidates
- event handlers may detect candidates
- only the rule evaluation service decides whether to create, refresh, suppress, or skip a task

This prevents worker-specific task semantics from diverging over time.

### 6.2 Components

#### `task_rules` configuration

Typed configuration defining:

- rule id
- trigger event types
- entity scope
- activation conditions
- dedupe key generator
- assignment strategy override if needed
- priority scoring inputs
- task template builder
- close-loop effects

#### Rule evaluation service

Responsible for:

- loading applicable rules for an event or scheduled scan
- building normalized rule context
- evaluating conditions deterministically
- generating `dedupe_key`, provenance fields, and snapshot
- resolving assignment
- computing priority score and mapped priority
- upserting or skipping the task

#### Task materialization layer

Responsible for:

- create vs refresh behavior
- terminal-state protection
- dedupe against active tasks
- idempotent retries

### 6.3 Scheduled Activation Contract

Scheduled activation is worker-driven, not lazy on read.

- A dedicated activation pass runs every 5 minutes in the worker.
- It promotes tasks from `scheduled` to `pending` when `scheduled_for <= now` in the office-local timezone.
- Pre-activation scheduled tasks do not appear in active sections, do not count toward overdue/today/upcoming totals, and are not included in actionable task queues.
- Once promoted, normal sectioning and overdue evaluation apply.
- If a scheduled task is already terminal when the activation pass sees it, it is skipped.

This keeps read paths simple and avoids hidden state mutation during list/count API calls.

### 6.4 Dedupe and Idempotency Contract

`dedupe_key` is the canonical persistence-level idempotency key for system-generated tasks.

Rules:

- manual tasks do not use `dedupe_key`
- system-generated tasks must set `dedupe_key`
- active-task uniqueness is enforced at the database layer

Database contract:

- add a partial unique index on active system tasks scoped to `(origin_rule, dedupe_key)`
- active means status in `scheduled`, `pending`, `in_progress`, `waiting_on`, `blocked`
- terminal tasks (`completed`, `dismissed`) do not participate in the active unique index

Conflict policy:

- on unique conflict, the evaluator refreshes the existing active task instead of creating a second task
- refresh updates `source_rule`, `source_event`, `reason_code`, `entity_snapshot`, priority, scheduling metadata, and assignment where allowed by policy
- refresh never reopens terminal tasks

This is the required protection against concurrent cron and event evaluation.

### 6.5 Rule Ownership and Refresh Semantics

Rule-driven tasks have explicit field ownership boundaries so background evaluation cannot silently overwrite human edits.

Rule-owned fields:

- `source_rule`
- `source_event`
- `reason_code`
- `entity_snapshot`
- `dedupe_key`
- `origin_rule`
- `office_id`

Rule-refreshable unless manually locked:

- `priority`
- `scheduled_for`
- `waiting_on`
- `blocked_by`

User-owned after first manual edit:

- `title`
- `description`
- `assigned_to`
- `due_date`
- `due_time`
- `remind_at`

Contract:

- manual edits to user-owned fields are never overwritten by evaluator refreshes
- if a rule wants to suggest a change to a user-owned field after manual ownership exists, it is surfaced as a recommendation or a new task state transition, not a silent overwrite
- evaluator refreshes may not move tasks between active non-terminal statuses; non-terminal status changes happen only through explicit lifecycle transitions or the scheduled activation rule described above
- system transitions into terminal states always win over refresh attempts
- implementations may use explicit manual-lock columns or equivalent metadata, but the behavior contract above is mandatory

### 6.6 Event Inputs

Initial triggers:

- `email.received`
- `deal.stage.changed`
- `deal.updated`
- `contact.created`
- `cron.daily_task_generation`
- `cron.cold_lead_warming`

### 6.7 Migration Strategy

Migrate existing hardcoded automations one source at a time behind the evaluator. Do not rewrite all jobs at once.

Order:

1. close-date reminders
2. first-outreach touchpoints
3. cadence follow-ups
4. stale deals
5. inbound email tasks
6. cold lead warming

Compatibility rule during migration:

- legacy worker/job code may continue detecting candidates
- only the rule evaluator may materialize or refresh the task row once a source is migrated
- source-specific direct inserts are removed immediately after that source is migrated
- each migrated source gets parity tests before removing legacy direct-write behavior

Each migration keeps current behavior unless the spec explicitly changes it.

---

## 7. Assignment Strategy

Assignment resolution is deterministic and ordered:

1. explicit manual override from the caller or rule
2. deal owner if the task is tied to a deal
3. contact-linked rep if the task is tied to a contact with an active deal relationship
4. recent actor on the entity when it indicates current ownership
5. office fallback user

Rules:

- reps can only assign tasks to themselves in manual flows
- directors/admins can filter and assign across available users for the active office
- office fallback must be explicit and testable, not “first row returned” behavior hidden inside a worker

Office fallback source of truth:

- add or use an office-level configured fallback assignee in office settings
- fallback must reference an active user with office access
- if the configured fallback is inactive or missing, the evaluator returns a structured skip reason and does not silently reassign to a random user
- per-rule overrides may replace the office fallback only when explicitly configured

If assignment cannot be resolved, the evaluator returns a structured skip reason rather than silently creating a broken task.

---

## 8. Prioritization Strategy

Priority is no longer hardcoded per worker path except where the rule explicitly overrides.

The evaluator computes a score from:

- due proximity
- deal stage risk
- stale age
- unread inbound activity
- deal value

Mapping:

- highest score band -> `urgent`
- high score band -> `high`
- medium score band -> `normal`
- low score band -> `low`

The exact scoring weights live in code/config and are tested. They should be easy to tune without rewriting worker logic.

Overdue tasks still surface first, but overdue status no longer substitutes for explicit priority logic.

---

## 9. API and UI Behavior

### 9.1 API

The tasks API is extended to:

- return the new lifecycle statuses
- expose new provenance and scheduling fields
- validate transition rules and companion-field requirements
- support assignee filtering for director/admin task views
- support status updates into `scheduled`, `in_progress`, `waiting_on`, and `blocked`

The API spec must be updated to match implementation and stop advertising stale task enums or types.

Section/count behavior:

- `scheduled` tasks are excluded from active counts and from overdue/today/upcoming sections until activation
- `pending`, `in_progress`, `waiting_on`, and `blocked` are active tasks
- `waiting_on` and `blocked` remain in the same date bucket as other active tasks, with explicit state badges and explanation payloads
- overdue status applies to active tasks with due dates in the past, including `waiting_on` and `blocked`
- completed counts include only `completed`
- dismissed tasks remain visible in task detail/history responses but never count toward completed totals in this initiative

Rationale:

- T Rock’s workflow treats waiting and blocked work as still-open commitments that must remain visible on dashboards until resolved
- keeping them in active buckets preserves operational accountability while the explicit state badge explains why the task is not currently executable

### 9.2 UI

Task UI changes:

- show assignee display names consistently
- expose lifecycle transitions beyond complete/dismiss/snooze
- show why a task is waiting or blocked
- show scheduled state distinctly from active work
- show scheduled tasks in a dedicated non-actionable scheduled view or filter, separate from active sections
- allow director/admin assignee filtering on the tasks page
- keep completed/dismissed tasks non-editable from the normal row interaction

The UI should favor explicit labels over implicit color-only meaning.

---

## 10. Production Hosting Scope

This initiative includes production hosting/path fixes only where they are directly actionable and tightly connected to task functionality.

In scope:

- frontend/API path mismatches that break task screens or task actions
- incorrect environment-derived URLs used by task-related notifications or deep links
- deployment configuration changes required to make task flows behave correctly in production

Out of scope:

- broad hosting/platform redesign not needed for task functionality
- unrelated deployment cleanup without direct task impact

If a larger hosting issue is discovered, document it and isolate it rather than letting it block the task engine work.

---

## 11. Email Routing Requirement

All system emails produced by task and notification automation in this initiative must route to `adnaan.iqbal@gmail.com`.

This applies to:

- overdue task notifications
- stale deal notifications
- inbound email alert notifications
- approval-needed notifications generated from this task/notification area
- any new task-rule-driven system emails added by this implementation

This does not change user-authored CRM email sending through Microsoft Graph. It only affects system-generated email delivery.

Implementation contract:

- add a notification-email recipient override in the notification/email-delivery path
- the override applies only to task-initiative notification emails: overdue task, stale deal, inbound email alert, approval-needed from this task/notification area, and any new task-rule-driven system emails introduced by this implementation
- for this initiative, the override is mandatory and routes those scoped system-notification emails to `adnaan.iqbal@gmail.com` regardless of the task assignee or notification recipient
- the override applies in one place, before email send, so templates and callers do not implement their own routing logic
- the implementation should use the existing single-recipient override mechanism in the Resend send path, but only from the task/notification delivery path that knows the notification type and can scope the override correctly
- CRM user-authored outbound email via Microsoft Graph bypasses this override entirely

---

## 12. Testing Strategy

This work is implemented with TDD.

Required coverage areas:

- lifecycle transition validation
- companion-field requirements for `scheduled`, `waiting_on`, and `blocked`
- assignment resolution order
- priority scoring and mapping
- dedupe key generation and idempotent task materialization
- concurrent event and cron races against the active-task unique index
- migration parity for existing automation paths
- mixed legacy and rule-driven rows during the compatibility window
- close-loop completion side effects
- suppression-state creation, expiry, and regeneration checks
- office-local scheduled activation including DST boundary cases
- task office persistence and office-derived activation behavior
- assignee display-name correctness in API/UI
- director/admin assignee filtering behavior
- system email routing override to `adnaan.iqbal@gmail.com`
- backfill correctness for provenance and dedupe fields where derivable
- task-related production path/deep-link behavior where changed

Testing layers:

- unit tests for rule evaluation, transitions, assignment, and scoring
- service tests for task create/update/complete flows
- worker integration tests for migrated automation sources
- client tests for lifecycle controls and assignee filtering

---

## 13. Risks and Controls

### Risk: rule migration changes behavior unintentionally

Control:

- migrate rule sources one by one
- encode parity expectations in tests before moving logic

### Risk: new statuses break existing UI assumptions

Control:

- add API validation first
- add explicit client rendering and actions for each non-terminal status

### Risk: dedupe becomes inconsistent across old and new paths

Control:

- move all automated creates through one materialization layer
- make `dedupe_key` the canonical dedupe mechanism

### Risk: assignment fallback stays opaque

Control:

- centralize assignment logic
- test each step in the resolution chain

### Risk: hosting issues expand the scope too far

Control:

- only fix task-adjacent deployment/path problems inside this initiative
- isolate larger hosting work as separate follow-up

---

## 14. Implementation Boundaries

This design intentionally does not include:

- a full AI planner
- natural-language rule authoring
- autonomous multi-step workflow orchestration
- broad notification-system redesign outside task-related behavior

The immediate objective is a smart, deterministic task engine that is reliable enough to support the operational workflow already defined in T Rock’s dashboards and CRM process.

---

## 15. Rollout and Migration Contract

This work is an in-place evolution of the current task module and requires an explicit rollout order.

### 15.1 Schema Migration Order

1. add new task enum values and new task columns, including `office_id`, `origin_rule`, and `dedupe_key`
2. add `task_resolution_state`
3. add indexes for active task querying and partial unique dedupe enforcement
4. deploy code that can read both pre-rule and rule-driven task rows
5. migrate automation sources one at a time behind the evaluator
6. remove legacy direct inserts after parity is verified for each source

### 15.2 Backfill Rules

- existing manual tasks get null provenance fields
- existing task rows get `office_id` backfilled from their tenant schema office at migration time
- existing system-generated tasks get best-effort `origin_rule`, `source_rule`, `reason_code`, and `dedupe_key` backfills only where they can be derived safely
- tasks without safe derivation remain null-provenance legacy rows and continue to function
- existing `pending` tasks are not auto-converted to `scheduled`, `waiting_on`, or `blocked` during migration

Backfill guarantees:

- `office_id` is fully backfilled because tenant schema context provides the source office deterministically
- `origin_rule` and `dedupe_key` are nullable for legacy rows when safe derivation is not possible

### 15.3 Compatibility Window

During migration:

- list/count APIs must support both legacy and rule-driven tasks
- worker jobs that have not yet migrated may still detect candidate work
- migrated sources must write only through the evaluator
- direct SQL inserts into `tasks` for migrated sources are considered a bug

### 15.4 Index Requirements

Required indexes include:

- assignee/status/date index for active list queries
- partial unique index on active `(origin_rule, dedupe_key)`
- index on `scheduled_for` for activation scans
- index on `origin_rule` and `reason_code` for diagnostics and reporting
- unique index on `task_resolution_state(origin_rule, dedupe_key)`

### 15.5 Cutover Safety

- each migrated source ships with parity tests and idempotency tests
- each migration verifies enum changes, index creation, and backfill results before the source cutover is considered complete
- production rollout should enable one migrated source at a time when possible
- if a migrated rule path misbehaves, the source can be disabled without rolling back the entire task subsystem
