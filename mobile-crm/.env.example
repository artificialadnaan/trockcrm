# Base URL of the trockcrm API, WITHOUT a trailing /api (the client appends it).
# Never commit the real host — the repo's pre-commit hook blocks it. Supply this as an EAS build env var
# for real builds, or a local .env for `npm run dev`.
EXPO_PUBLIC_API_BASE_URL=https://<prod-api-host>
